theme: minima

# 한글 지원
lang: ko-KR

# 플러그인 설정 (GitHub Pages 기본 지원)
plugins:
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-sitemap

# 포스트 설정
paginate: 5
paginate_path: "/page:num/"

# Markdown 설정
markdown: kramdown
highlighter: rouge
